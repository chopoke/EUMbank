# ===================================================================
# SERVER CONFIGURATION
# ===================================================================
server:
  port: 8081 # .properties 파일의 설정 적용

# ===================================================================
# SPRING FRAMEWORK CONFIGURATION
# ===================================================================
spring:
  application:
    name: eum_bank # .properties 파일의 설정 적용

  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.http.client.HttpClientAutoConfiguration
      - org.springframework.boot.autoconfigure.web.client.RestClientAutoConfiguration
  # --- Database Connection (MariaDB) ---
  # 기존 .yml 파일의 MariaDB 설정을 유지합니다.
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver

    url: jdbc:mariadb://localhost:3306/eum_bank?sessionVariables=time_zone='+09:00'
    username: ${DB_USER} # 환경 변수에서 주입
    password: ${DB_PASS} # 환경 변수에서 주입
    hikari:
      maximum-pool-size: ${DB_POOL_MAX:10}
      minimum-idle: ${DB_POOL_MIN:2}

  # --- JPA & Hibernate ---
  # .properties 파일의 설정 기준으로 병합되었습니다.
  jpa:
    database-platform: org.hibernate.dialect.MariaDBDialect
    hibernate:
      ddl-auto: validate # none -> validate로 변경
    open-in-view: false
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: Asia/Seoul

  # --- Mail Sender (Gmail SMTP) ---
  # .properties 파일에서 가져온 새로운 설정입니다.
  mail:
    host: smtp.gmail.com
    port: 587
    username: amumu7700@gmail.com
    password: rkiixxqyoaqiqdyp
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
    default-encoding: UTF-8

  # --- File Upload ---
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

  # --- JSON & Timezone ---
  jackson:
    time-zone: Asia/Seoul
    serialization:
      write-dates-as-timestamps: false

  # ===================================================================
  # OAuth2 : NAVER, GOOGLE
  # ===================================================================

  security:
    oauth2:
      client:
        registration:
          naver:
            client-id: dYMyrfa3rgIKuDwUYZyH
            client-secret: HwVQiFUqAQ
            scope:
              - name
              - email
            client-name: naver
            authorization-grant-type: authorization_code
            redirect-uri: http://localhost:8081/login/oauth2/code/naver
          google:
            client-name: google
            client-id: 1009179449495-uqphuqqd21h1919m4354agk1cjottn33.apps.googleusercontent.com
            client-secret: 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
            redirect-uri: http://localhost:8081/login/oauth2/code/google
            authorization-grant-type: authorization_code
            scope:
              - profile
              - email
        provider:
          naver:
            authorization-uri: https://nid.naver.com/oauth2.0/authorize
            token-uri: https://nid.naver.com/oauth2.0/token
            user-info-uri: https://openapi.naver.com/v1/nid/me
            user-name-attribute: response

# ===================================================================
# MYBATIS CONFIGURATION
# ===================================================================
mybatis:
  mapper-locations: classpath*:mapper/**/*.xml
  type-aliases-package: com.boot.eumbank.dto
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true

# ===================================================================
# CUSTOM APPLICATION PROPERTIES
# ===================================================================
app:
  # --- CORS ---
  cors:
    allowed-origins:
      - http://localhost:3000
  # --- Email Verification ---
  email:
    verify:
      expire-minutes: 15
    code:
      secret: K7k6x7w4Y7xV1X7y0i3o9kq2PqZB1k4mQ9l2s6u8v0w3x5y7z9A==
      exp-min: 15
      period-seconds: 60
      clock-skew: 1

# ===================================================================
# EXTERNAL API CONFIGURATION
# ===================================================================
# --- Naver Clova OCR ---
clova:
  ocr:
    url: ${CLOVA_OCR_URL}
    secret: ${CLOVA_OCR_SECRET}

# --- Korea EXIM API : 한국수출입은행에서 환율(매매/기준환율) 데이터를 내려주는 공개 API ---
exim:
  api:
    base: https://www.koreaexim.go.kr/site/program/financial/exchangeJSON
    key: GWHvpofCSHfJ4eumZdAavLgNkl8J4qme

# ===================================================================
# JWT (JSON Web Token) CONFIGURATION
# ===================================================================
jwt:
  access:
    secret: change-this-to-a-very-long-random-access-secret-at-least-64-bytes
    ttl-seconds: 36000
  refresh:
    secret: change-this-to-a-very-long-random-refresh-secret-at-least-64-bytes
    ttl-seconds: 86400

# ===================================================================
# LOGGING CONFIGURATION
# ===================================================================
logging:
  level:
    com.boot.eumbank.mapper: DEBUG
    org.apache.ibatis: DEBUG
    org.mybatis.spring: DEBUG

# Actuator
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always


# ===================================================================
# 금감원 대출API
# ===================================================================
finlife:
  base-url: "https://finlife.fss.or.kr"
  api-key: "b8026175e9f7ea9382ca8f19aa8c3fe7"

jobs:
  finlife:
    sync:
      cron: "0 */2 * * * *"    # 매일 03:00  --> 임시로 15초마다
  timezone: "Asia/Seoul"

# ==================================================================
# 공과금 API
# ==================================================================
kepco:
  endpoint: https://bigdata.kepco.co.kr/openapi/v1/powerUsage/contractType.do
  service-key: ${KEPCO_SERVICE_KEY}
  connect-timeout-ms: 2000
  read-timeout-ms: 3000