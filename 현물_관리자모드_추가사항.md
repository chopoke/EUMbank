# 현물 관리자모드 추가 사항

## 개요
현물(금/은) 거래 관리자 모드 기능이 추가되었습니다. 관리자 대시보드에서 현물 관련 통계 및 회원별 보유량을 조회할 수 있습니다.

---

## 백엔드 (Java/Spring Boot) 추가 사항

### 1. 컨트롤러

#### `SpotAdminController`
**경로**: `src/main/java/com/boot/eumbank/spot/admin/controller/SpotAdminController.java`

- **역할**: 현물 관리자용 REST API 제공
- **기본 경로**: `/api/admin/spot`
- **권한**: `@PreAuthorize("hasRole('ADMIN')")` - 관리자 권한 필요

**주요 엔드포인트**:
- `GET /api/admin/spot/statistics` - 현물 통계 데이터 조회
- `GET /api/admin/spot/users` - 회원별 현물 보유량 목록 조회

**특징**:
- 기존 `AdminDashboardController`와 분리하여 병합 시 충돌 최소화
- REST API 방식으로 프론트엔드와 통신

#### `AdminDashboardController` (통합)
**경로**: `src/main/java/com/boot/eumbank/admin/controller/AdminDashboardController.java`

- **역할**: 관리자 대시보드 페이지 렌더링 및 통계 데이터 제공
- **기본 경로**: `/admin/dashboard`

**변경 사항**:
- `SpotAdminService` 주입 추가 (`@RequiredArgsConstructor` 사용)
- 금/은 보유량 통계 카드(7번, 8번)에 실제 데이터 적용
- `SpotAdminService.getSpotStatistics()`를 통해 현물 통계 조회
- 금/은 보유량을 그램 단위에서 kg 단위로 변환하여 표시

**구현 내용**:
```java
// 7. 금 보유량
// 현물 통계 데이터 조회 및 변환
SpotAdminStatisticsDTO spotStats = spotAdminService.getSpotStatistics();
String goldFormatted = "0.00 kg";
if (spotStats != null && spotStats.getTotalGoldBalance() != null) {
    BigDecimal goldInKg = spotStats.getTotalGoldBalance()
            .divide(new BigDecimal("1000"), 2, RoundingMode.HALF_UP);
    goldFormatted = String.format("%,.2f kg", goldInKg);
}
stats.add(Map.of("title", "금 보유량",
               "value", goldFormatted,
               "icon", "ri-stack-line", 
               "color", "text-yellow-600",
               "iconColor", "gold"));

// 8. 은 보유량
// 동일한 spotStats 변수 재사용 (한 번만 조회)
String silverFormatted = "0.00 kg";
if (spotStats != null && spotStats.getTotalSilverBalance() != null) {
    BigDecimal silverInKg = spotStats.getTotalSilverBalance()
            .divide(new BigDecimal("1000"), 2, RoundingMode.HALF_UP);
    silverFormatted = String.format("%,.2f kg", silverInKg);
}
stats.add(Map.of("title", "은 보유량",
               "value", silverFormatted,
               "icon", "ri-stack-line", 
               "color", "text-gray-600",
               "iconColor", "silver"));
```

**최적화**:
- `getSpotStatistics()`를 한 번만 호출하여 `spotStats` 변수 재사용
- 금/은 보유량 조회 로직을 7번, 8번 항목 바로 위에 배치하여 가독성 향상
- `ArrayList`를 사용하여 동적으로 항목 추가

---

### 2. 서비스 계층

#### `SpotAdminService` (인터페이스)
**경로**: `src/main/java/com/boot/eumbank/spot/admin/service/SpotAdminService.java`

**메서드**:
- `SpotAdminStatisticsDTO getSpotStatistics()` - 현물 통계 데이터 조회
- `List<SpotUserHoldingsDTO> getUserHoldings()` - 고객별 현물 보유량 목록

#### `SpotAdminServiceImpl` (구현체)
**경로**: `src/main/java/com/boot/eumbank/spot/admin/service/SpotAdminServiceImpl.java`

**주요 기능**:

1. **현물 통계 조회 (`getSpotStatistics`)**:
   - 지갑 통계: 총 지갑 개수, 현금/금/은 잔고 합계
   - 현재 시세 조회: 금(AU), 은(AG) 현재 시세
   - 평가액 계산: 금/은 평가액 및 총 현물 자산
   - 거래 통계: 총 거래 건수 및 거래액
   - 고객 통계: 현물 고객 수, 총 투자금액, 총 손익

2. **회원별 보유량 조회 (`getUserHoldings`)**:
   - 고객별 금/은 보유량 집계
   - `GoldWalletRepository.findAllWithCustomer()` 사용
   - 고객 번호별로 그룹화하여 합계 계산

**의존성**:
- `GoldWalletRepository` - 지갑 데이터 조회
- `GoldTblRepository` - 거래 데이터 조회
- `GoldCustomerRepository` - 고객 데이터 조회
- `PriceService` - 현재 시세 조회

**에러 처리**:
- 예외 발생 시 기본값(0 또는 빈 리스트) 반환
- 로그 기록 (`@Slf4j`)

---

### 3. DTO (Data Transfer Object)

#### `SpotAdminStatisticsDTO`
**경로**: `src/main/java/com/boot/eumbank/spot/admin/dto/SpotAdminStatisticsDTO.java`

**필드**:
- `Long totalWalletCount` - 현물 지갑 총 개수
- `BigDecimal totalCashBalance` - 현물 현금 잔고 총액 (원화)
- `BigDecimal totalGoldBalance` - 금 보유량 총합 (그램)
- `BigDecimal totalSilverBalance` - 은 보유량 총합 (그램)
- `BigDecimal currentGoldPrice` - 현재 금 시세 (그램당)
- `BigDecimal currentSilverPrice` - 현재 은 시세 (그램당)
- `BigDecimal totalGoldEvaluation` - 금 평가액 총합
- `BigDecimal totalSilverEvaluation` - 은 평가액 총합
- `BigDecimal totalSpotAssets` - 현물 총 자산 (현금 + 금 평가액 + 은 평가액)
- `Long totalTransactionCount` - 현물 거래 총 건수
- `BigDecimal totalTransactionAmount` - 현물 거래 총액
- `Long totalSpotCustomerCount` - 현물 고객 수
- `BigDecimal totalInvestment` - 현물 총 투자금액
- `BigDecimal totalProfitLoss` - 현물 총 손익

#### `SpotUserHoldingsDTO`
**경로**: `src/main/java/com/boot/eumbank/spot/admin/dto/SpotUserHoldingsDTO.java`

**필드**:
- `Long customerNo` - 고객 번호
- `String userId` - 사용자 ID
- `BigDecimal gold` - 금 보유량 (그램)
- `BigDecimal silver` - 은 보유량 (그램)

---

### 4. Repository 확장

#### `GoldWalletRepository`
**경로**: `src/main/java/com/boot/eumbank/spot/repository/GoldWalletRepository.java`

**추가된 메서드**:
```java
@Query("SELECT gw FROM GoldWallet gw WHERE gw.customer IS NOT NULL")
List<GoldWallet> findAllWithCustomer();
```

- 고객 정보가 있는 지갑만 조회
- 회원별 보유량 집계에 사용

---

## 프론트엔드 (React) 추가 사항

### 1. 컴포넌트

#### `SpotManagement.jsx`
**경로**: `front_end/src/pages/admin/components/SpotManagement.jsx`

**기능**:
- 현물 통계 데이터 조회 및 표시
- 회원별 금/은 보유량 테이블 표시

**주요 상태**:
- `summary`: 현물 통계 데이터
- `users`: 회원별 보유량 목록
- `loading`: 로딩 상태
- `error`: 에러 메시지

**API 호출**:
- `GET /api/admin/spot/statistics` - 통계 데이터
- `GET /api/admin/spot/users` - 회원별 보유량

**UI 구성**:
1. **헤더**: "현물 관리" 제목 및 설명
2. **통계 카드**:
   - Total Gold Holdings (전체 금 보유량)
   - Total Silver Holdings (전체 은 보유량)
3. **회원별 보유량 테이블**:
   - Customer No
   - User ID
   - Gold (g)
   - Silver (g)

**에러 처리**:
- 401 에러: "관리자 권한이 필요합니다."
- 기타 에러: "현물 통계를 불러오지 못했습니다."

---

### 2. 관리자 페이지 통합

#### `AdminPage` (page.jsx)
**경로**: `front_end/src/pages/admin/page.jsx`

**변경 사항**:
- `SpotManagement` 컴포넌트 import 추가
- 대시보드에 `SpotManagement` 컴포넌트 통합
- 메뉴에서 'spot' 선택 시 `SpotManagement` 표시

```jsx
case 'dashboard':
  return (
    <>
      <Dashboard />
      <div className="mt-10">
        <SpotManagement />
      </div>
    </>
  );
case 'spot':
  return <SpotManagement />;
```

---

### 3. 사이드바 메뉴

#### `Sidebar.jsx`
**경로**: `front_end/src/pages/admin/components/Sidebar.jsx`

**추가된 메뉴 항목**:
```jsx
{ id: "spot", label: "현물관리", icon: "ri-coins-line" }
```

**변경 사항**:
- `reactMenus` Set에 "spot" 추가
- 메뉴 아이템 배열에 현물관리 항목 추가
- 아이콘: `ri-coins-line` (RemixIcon)

---

## 파일 구조

```
src/main/java/com/boot/eumbank/spot/admin/
├── controller/
│   └── SpotAdminController.java          [신규]
├── service/
│   ├── SpotAdminService.java             [신규]
│   └── SpotAdminServiceImpl.java         [신규]
└── dto/
    ├── SpotAdminStatisticsDTO.java        [신규]
    └── SpotUserHoldingsDTO.java           [신규]

src/main/java/com/boot/eumbank/spot/repository/
└── GoldWalletRepository.java              [수정: findAllWithCustomer() 추가]

src/main/java/com/boot/eumbank/admin/controller/
└── AdminDashboardController.java          [수정: SpotAdminService 통합, 금/은 보유량 실제 데이터 적용]

front_end/src/pages/admin/
├── page.jsx                                [수정: SpotManagement 통합]
└── components/
    ├── Sidebar.jsx                         [수정: 현물관리 메뉴 추가]
    └── SpotManagement.jsx                  [신규]
```

---

## API 명세

### 1. 현물 통계 조회

**요청**:
```
GET /api/admin/spot/statistics
Authorization: Bearer {token} (관리자 권한 필요)
```

**응답**:
```json
{
  "totalWalletCount": 150,
  "totalCashBalance": 50000000.00,
  "totalGoldBalance": 1250.500,
  "totalSilverBalance": 5430.750,
  "currentGoldPrice": 85000.00,
  "currentSilverPrice": 1200.00,
  "totalGoldEvaluation": 106292500.00,
  "totalSilverEvaluation": 6516900.00,
  "totalSpotAssets": 161809400.00,
  "totalTransactionCount": 500,
  "totalTransactionAmount": 200000000.00,
  "totalSpotCustomerCount": 120,
  "totalInvestment": 180000000.00,
  "totalProfitLoss": -18190600.00
}
```

---

### 2. 회원별 보유량 조회

**요청**:
```
GET /api/admin/spot/users
Authorization: Bearer {token} (관리자 권한 필요)
```

**응답**:
```json
[
  {
    "customerNo": 1,
    "userId": "user001",
    "gold": 10.500,
    "silver": 25.250
  },
  {
    "customerNo": 2,
    "userId": "user002",
    "gold": 5.000,
    "silver": 15.750
  }
]
```

---

## 보안

- 모든 API 엔드포인트는 `@PreAuthorize("hasRole('ADMIN')")`로 보호됨
- 관리자 권한이 없는 사용자는 401 Unauthorized 응답 수신
- 프론트엔드에서도 401 에러 시 적절한 메시지 표시

---

## 성능 최적화

1. **데이터베이스 쿼리 최적화**:
   - `findAll()`을 한 번만 호출하여 재사용
   - 불필요한 반복 쿼리 방지

2. **서비스 호출 최적화**:
   - `AdminDashboardController`에서 `getSpotStatistics()`를 한 번만 호출
   - 동일한 `spotStats` 변수를 금/은 보유량 계산에 재사용

3. **프론트엔드 최적화**:
   - `Promise.all()`을 사용하여 두 API를 병렬 호출
   - 컴포넌트 언마운트 시 요청 취소 (`mounted` 플래그)

---

## 주의사항

1. **시세 조회 실패 처리**:
   - 금/은 시세 조회 실패 시 0으로 설정
   - 로그에 경고 메시지 기록

2. **데이터 정확도**:
   - `BigDecimal` 사용으로 정확한 금액 계산
   - 소수점 처리: 현금/평가액은 2자리, 금/은 보유량은 3자리
   - 그램 → kg 변환 시 `RoundingMode.HALF_UP` 사용

3. **에러 처리**:
   - 서비스 레이어에서 예외 발생 시 기본값 반환
   - 프론트엔드에서 에러 상태 표시
   - `AdminDashboardController`에서 null 체크 후 기본값("0.00 kg") 설정

4. **단위 변환**:
   - 데이터베이스에는 그램(g) 단위로 저장
   - 대시보드 표시 시 kg 단위로 변환 (1000으로 나누기)
   - 포맷팅: `String.format("%,.2f kg", value)`

---

## 향후 개선 사항

1. **페이징 처리**: 회원별 보유량 목록에 페이징 추가
2. **필터링/검색**: 고객 번호, 사용자 ID로 검색 기능
3. **정렬**: 보유량 기준 정렬 기능
4. **차트/그래프**: 통계 데이터 시각화
5. **엑셀 다운로드**: 회원별 보유량 엑셀 내보내기

---

## 작성일
2024년 (작성 시점 기준)

## 작성자
현물 관리자모드 개발팀

